apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: tripkey-staging

namespace: tripkey-staging

resources:
  - ../../base
  - configmap.yaml

patches:
  - path: ingress-patch.yaml
  - path: replicas-patch.yaml
