apiVersion: v1
kind: Service
metadata:
  name: tripkey-ai-worker
  labels:
    app.kubernetes.io/name: tripkey
    app.kubernetes.io/component: ai-worker
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: tripkey
    app.kubernetes.io/component: ai-worker
  ports:
    - name: http
      port: 8080
      targetPort: http

