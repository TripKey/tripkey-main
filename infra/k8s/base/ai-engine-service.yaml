apiVersion: v1
kind: Service
metadata:
  name: tripkey-ai-engine
  labels:
    app.kubernetes.io/name: tripkey
    app.kubernetes.io/component: ai-engine
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: tripkey
    app.kubernetes.io/component: ai-engine
  ports:
    - name: http
      port: 8000
      targetPort: http

