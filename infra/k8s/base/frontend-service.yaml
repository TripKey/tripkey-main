apiVersion: v1
kind: Service
metadata:
  name: tripkey-frontend
  labels:
    app.kubernetes.io/name: tripkey
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: tripkey
    app.kubernetes.io/component: frontend
  ports:
    - name: http
      port: 80
      targetPort: http

