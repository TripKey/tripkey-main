# Vite dev proxy 기준 (필요 시만)
VITE_API_BASE_URL=/api

# Vite dev server가 /api 요청을 프록시할 백엔드 주소.
# 로컬에서 백엔드를 직접 실행할 때
VITE_DEV_PROXY_TARGET=http://localhost:8080

# Docker Compose 안에서 띄울 때
# VITE_DEV_PROXY_TARGET=http://backend:8080
